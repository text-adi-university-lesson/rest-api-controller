services:
  cloudflare-tunnel:
    image: cloudflare/cloudflared:latest
    command: tunnel run
    env_file:
      - .env
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    depends_on:
      - app

  app:
    build:
      dockerfile: Dockerfile
      context: .
      target: prod
    expose:
      - 3000
    env_file:
      - .env